import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.contact.example',
  appName: 'Contact Example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
